export type Time = {
  s: string,
  y: number,
  m: number,
  d: number,
  // desc: string,
  // festival: string,
  // dark: boolean,
  // isWork: boolean
}

export type TimeInfo = {
  s: string,
  y: number,
  m: number,
  d: number,
  desc: string,
  festival: string,
  dark: boolean,
  isWork: boolean,
  current: boolean
}

export type Work = {
  a: number,
  b: number,
  c: number,
  l: boolean,
  o: string,
  s: string
}
